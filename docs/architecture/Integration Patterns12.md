[{'name': 'Request-Reply', 'type': 'Communication', 'implementation': 'Synchronous HTTP/REST between the client and server.', 'applicability': 'This is the essential pattern for all interactions between the React frontend and the ASP.NET Core backend. It directly supports requirements like REQ-INTG-DTR-001, where the client sends a request (e.g., to preview a script) and waits for a synchronous response (the transformed JSON or a structured error).'}, {'name': 'API Gateway', 'type': 'Structural', 'implementation': 'The ASP.NET Core API/Web Layer functions as an embedded API Gateway.', 'applicability': 'Provides a single, unified entry point for the React client. It is essential for centralizing cross-cutting concerns like JWT authentication, Role-Based Access Control (RBAC) authorization, request validation, and routing to the appropriate Application Layer services, simplifying both the client and the backend logic.'}, {'name': 'Timeout', 'type': 'Reliability', 'implementation': 'Server-side timeout enforcement using CancellationToken on specific API endpoints.', 'applicability': 'Directly fulfills the explicit requirement REQ-PERF-DTR-001. This pattern is essential to protect the backend service from resource exhaustion caused by long-running or malicious user-provided scripts, ensuring system stability and reliability.'}, {'name': 'Exception Shielding', 'type': 'Security', 'implementation': 'A global exception handling middleware within the ASP.NET Core pipeline.', 'applicability': 'This pattern is essential to fulfill REQ-FUNC-DTR-003. It catches all unhandled exceptions, preventing the leakage of sensitive internal details (like stack traces), logs the full error for diagnostics, and returns a standardized, safe JSON error structure to the client, providing a consistent and secure error handling contract.'}, {'name': 'Caching', 'type': 'Performance', 'implementation': 'Distributed caching using Redis for non-transactional, frequently-read data.', 'applicability': "Explicitly defined in the architecture's performance tactics, this pattern is essential for reducing database load and improving API response times. It is used for application configuration values and user roles to support scalable, high-performance authentication and authorization checks on every API call."}]

