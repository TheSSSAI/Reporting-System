# -----------------------------------------------------------------------------
# Reporting System - Web UI Environment Variables
# -----------------------------------------------------------------------------
# This file serves as an example for the .env.local file.
# Copy this file to .env.local and fill in the appropriate values for your
# development environment. This file should NOT contain production secrets.
# -----------------------------------------------------------------------------

# The base URL for the backend API.
# During development, this is used by the Vite proxy to forward API requests
# to the running backend service, avoiding CORS issues.
# In production, this can be a relative path if the UI is served by the same host,
# or a full URL if served from a different domain.
VITE_API_BASE_URL=http://localhost:5000

# Application title, displayed in the browser tab.
VITE_APP_TITLE="Reporting System"